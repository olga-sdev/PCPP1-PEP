"""
Use Python’s default encodings -> Python 3 -- UTF-8, Python 2 -- ASCII.

imports -> on separate lines.

Use absolute imports.

imports at the beginning of the script.

Avoid using wildcard imports: `from module import *`
"""

# Example of import on separate line
import pandas as pd

# Example of from … import … syntax
from selenium import webdriver

# Example with absolute imports
from config.settings import Config
