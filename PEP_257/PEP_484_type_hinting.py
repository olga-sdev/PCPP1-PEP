"""
Type hiting -> additional information without comments, statically indicate the type information related to Python objects.

It reduces the risk of passing a wrong type of value to the function.

Type hinting is optional.
"""


def show_time(day: int) -> int:
    return day
